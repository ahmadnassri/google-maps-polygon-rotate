{
  "private": true,
  "name": "google-maps-rotate",
  "version": "1.0.2",
  "description": "Rotate a google.maps.Polygon on Google Maps V3 relative to a google.maps.LatLng",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "venus run --test tests --coverage --phantom",
    "venus": "venus run --test tests"
  },
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-jshint": "^1.0.0",
    "grunt-contrib-uglify": "^3.0.1",
    "load-grunt-tasks": "^3.5.2",
    "time-grunt": "^1.0.0",
    "venus": "^2.3.4"
  }
}
